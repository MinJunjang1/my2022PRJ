//서울
function setSeoul() {
    num1 = 37.541;
    num2 = 126.986;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();

}
//종로구
function setJongnogu() {
    num1 = 37.57037777777778;
    num2 = 126.98164166666668;

    // 이동할 위도 경도 위치를 생성합니다
    var moveLatLon = new kakao.maps.LatLng(num1, num2);
    map.setCenter(moveLatLon);
    var level = map.getLevel();
    // 지도를 1레벨 내립니다 (지도가 확대됩니다)
    map.setLevel(15);
    // 지도 레벨을 표시합니다
    displayLevel();
}
//중구
function setJunggu() {
    num1 = 37.56100277777777;
    num2 = 126.99964166666666;

    // 이동할 위도 경도 위치를 생성합니다
    var moveLatLon = new kakao.maps.LatLng(num1, num2);
    map.setCenter(moveLatLon);
    var level = map.getLevel();
    // 지도를 1레벨 내립니다 (지도가 확대됩니다)
    map.setLevel(15);
    // 지도 레벨을 표시합니다
    displayLevel();
}
//용산구
function setYongsangu() {
    num1 = 37.536094444444444;
    num2 = 126.96752222222223;

    // 이동할 위도 경도 위치를 생성합니다
    var moveLatLon = new kakao.maps.LatLng(num1, num2);
    map.setCenter(moveLatLon);
    var level = map.getLevel();
    // 지도를 1레벨 내립니다 (지도가 확대됩니다)
    map.setLevel(15);
    // 지도 레벨을 표시합니다
    displayLevel();
}
//성동구
function setSeongdonggu() {
    num1 = 37.56061111111111;
    num2 = 127.039;

    // 이동할 위도 경도 위치를 생성합니다
    var moveLatLon = new kakao.maps.LatLng(num1, num2);
    map.setCenter(moveLatLon);
    var level = map.getLevel();
    // 지도를 1레벨 내립니다 (지도가 확대됩니다)
    map.setLevel(15);
    // 지도 레벨을 표시합니다
    displayLevel();
}
//광진구
    function setGwangjingu() {
        num1 = 37.535738888888886;
        num2 = 127.08453333333333;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//동대문구
    function setDongdaemungu() {
        num1 = 37.571625000000004;
        num2 = 127.04214166666667;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//중랑구
    function setJungnanggu() {
        num1 = 37.60380555555556;
        num2 = 127.09477777777778;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//성북구
    function setSeongbukgu() {
        num1 = 37.58638333333334;
        num2 = 127.02033333333333

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();

    }

//강북구
    function setGangbukgu() {
        num1 = 37.63695555555555;
        num2 = 127.02771944444444;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//도봉구
    function setDobonggu() {
        // 이동할 위도 경도 위치를 생성합니다
        num1 = 37.66583333333333;
        num2 = 127.04952222222222;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();

    }

//노원구
    function setNowongu() {
        num1 = 37.65146111111111;
        num2 = 127.05838888888889;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();

    }

//은평구
    function setEunpyeonggu() {
        num1 = 37.59996944444445;
        num2 = 126.93124166666666;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();

    }

//서대문구
    function setSeodaemungu() {
        num1 = 37.57636666666667;
        num2 = 126.93889722222222;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();

    }

//마포구
    function setMapogu() {
        num1 = 37.56070555555555;
        num2 = 126.91053055555557;


        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//양천구
    function setYangcheongu() {
        num1 = 37.51423055555556;
        num2 = 126.86870833333333;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//강서구
    function setGangseogu() {
        num1 = 37.54815555555555;
        num2 = 126.851675;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//구로구
    function setGurogu() {
        num1 = 37.49265;
        num2 = 126.88959722222224;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//금천구
    function setGeumcheongu() {
        num1 = 37.44910833333333;
        num2 = 126.90419722222222;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//영등포구
    function setYeongdeungpogu() {
        num1 = 37.52361111111111;
        num2 = 126.89834166666667;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//동작구
    function setDongjakgu() {
        num1 = 37.509655555555554;
        num2 = 126.941575;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//관악구
    function setGwanakgu() {
        num1 = 37.47538611111111;
        num2 = 126.95384444444444;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//서초구
    function setSeochogu() {
        num1 = 37.480786111111115;
        num2 = 127.03481111111111;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//강남구
    function setGangnamgu() {
        num1 = 37.514575;
        num2 = 127.04955555555556;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//송파구
    function setSongpagu() {
        num1 = 37.51175555555555;
        num2 = 127.10793055555555;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

//강동구
    function setGangdonggu() {
        num1 = 37.527366666666666;
        num2 = 127.12586388888889;

        // 이동할 위도 경도 위치를 생성합니다
        var moveLatLon = new kakao.maps.LatLng(num1, num2);
        map.setCenter(moveLatLon);
        var level = map.getLevel();
        // 지도를 1레벨 내립니다 (지도가 확대됩니다)
        map.setLevel(15);
        // 지도 레벨을 표시합니다
        displayLevel();
    }

